<Window x:Class="Rune.InstallerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Rune Setup"
        Width="640"
        Height="440"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="False"
        Background="#0F1117"
        Foreground="#F0F2F5"
        Icon="Assets/rune.ico">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="36"
                            ResizeBorderThickness="0"
                            GlassFrameThickness="0"
                            UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#7C3AED"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#6D28D9"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#F0F2F5"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style x:Key="CaptionButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#9CA3AF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#E81123"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="36"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Custom Titlebar -->
        <Grid Grid.Row="0" Background="#0B0C0F" MouseLeftButtonDown="Titlebar_MouseLeftButtonDown">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="12,0,0,0" VerticalAlignment="Center">
                <Image Source="Assets/rune.ico" Width="16" Height="16"/>
                <TextBlock Text="Rune Setup" Foreground="#9CA3AF" FontSize="12" Margin="8,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>

            <Button Grid.Column="2" Style="{StaticResource CaptionButton}"
                    Content="&#x2715;" Click="CloseBtn_Click"/>
        </Grid>

        <!-- Content -->
        <Grid Grid.Row="1" Margin="32,16,32,24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="Rune Setup Wizard" FontSize="20" FontWeight="Bold" Foreground="#F0F2F5"/>
                <TextBlock Text="Install Rune code editor and configure system shortcuts." FontSize="12" Foreground="#9CA3AF" Margin="0,4,0,0"/>
            </StackPanel>

            <!-- Content Area -->
            <Grid Grid.Row="1">
                <!-- Step 1: Options -->
                <StackPanel x:Name="Step1Panel" VerticalAlignment="Center">
                    <TextBlock Text="Installation Options" FontSize="14" FontWeight="SemiBold" Foreground="#F0F2F5" Margin="0,0,0,16"/>

                    <StackPanel Margin="0,0,0,16">
                        <TextBlock Text="Destination Folder:" FontSize="12" Foreground="#9CA3AF" Margin="0,0,0,6"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="PathTextBox" Padding="8" Background="#161922" Foreground="#F0F2F5" BorderBrush="#2C3242" BorderThickness="1" VerticalContentAlignment="Center"/>
                            <Button Grid.Column="1" Content="Browse..." Margin="8,0,0,0" Padding="12,6" Click="BrowseBtn_Click"/>
                        </Grid>
                    </StackPanel>

                    <StackPanel>
                        <CheckBox x:Name="DesktopShortcutCheck" Content="Create a desktop shortcut" IsChecked="True" Margin="0,0,0,10"/>
                        <CheckBox x:Name="StartMenuCheck" Content="Create a Start Menu shortcut" IsChecked="True"/>
                    </StackPanel>
                </StackPanel>

                <!-- Step 2: Installing -->
                <StackPanel x:Name="Step2Panel" VerticalAlignment="Center" Visibility="Collapsed">
                    <TextBlock Text="Installing Rune..." FontSize="16" FontWeight="SemiBold" Foreground="#F0F2F5" Margin="0,0,0,16"/>
                    <ProgressBar x:Name="InstallProgress" Height="6" IsIndeterminate="True" Background="#161922" Foreground="#7C3AED" BorderThickness="0" Margin="0,0,0,16"/>
                    <TextBlock x:Name="StatusText" Text="Extracting UI assets and creating configuration..." FontSize="12" Foreground="#9CA3AF"/>
                </StackPanel>

                <!-- Step 3: Finished -->
                <StackPanel x:Name="Step3Panel" VerticalAlignment="Center" Visibility="Collapsed">
                    <TextBlock Text="Installation Complete!" FontSize="18" FontWeight="Bold" Foreground="#10B981" Margin="0,0,0,10"/>
                    <TextBlock Text="Rune has been successfully installed." FontSize="13" Foreground="#9CA3AF" TextWrapping="Wrap" Margin="0,0,0,12"/>
                    <Border Background="#161922" CornerRadius="6" Padding="12">
                        <TextBlock x:Name="ReportText" FontSize="11" Foreground="#6B7280" TextWrapping="Wrap" FontFamily="Consolas"/>
                    </Border>
                </StackPanel>
            </Grid>

            <!-- Footer Buttons -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button x:Name="CancelBtn" Content="Cancel" Background="#262B3A" Click="CancelBtn_Click" Margin="0,0,10,0"/>
                <Button x:Name="NextBtn" Content="Install" Click="NextBtn_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
