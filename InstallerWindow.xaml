<Window x:Class="Rune.InstallerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Rune Setup"
        Width="640"
        Height="460"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="False"
        Background="#0F1117"
        Foreground="#F0F2F5"
        Icon="Assets/rune.ico"
        FontFamily="Segoe UI, Inter, sans-serif">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="40"
                            ResizeBorderThickness="0"
                            GlassFrameThickness="-1"
                            UseAeroCaptionButtons="False"
                            NonClientFrameEdges="None" />
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#7C3AED"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="18,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#6D28D9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#5B21B6"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Background" Value="#262B3A"/>
                                <Setter Property="Foreground" Value="#6B7280"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#161922"/>
            <Setter Property="Foreground" Value="#F0F2F5"/>
            <Setter Property="BorderBrush" Value="#2C3242"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="CaretBrush" Value="#7C3AED"/>
            <Setter Property="SelectionBrush" Value="#7C3AED"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#7C3AED"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#F0F2F5"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
        <Style x:Key="CaptionButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#9CA3AF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#E81123"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ProgressBar">
            <Setter Property="Background" Value="#1E222E"/>
            <Setter Property="Foreground" Value="#7C3AED"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid>
                            <Border Background="{TemplateBinding Background}" CornerRadius="3"/>
                            <Border x:Name="PART_Track" CornerRadius="3"/>
                            <Border x:Name="PART_Indicator" HorizontalAlignment="Left"
                                    Background="{TemplateBinding Foreground}" CornerRadius="3"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Custom Titlebar -->
        <Grid Grid.Row="0" Background="#0B0C0F" MouseLeftButtonDown="Titlebar_MouseLeftButtonDown">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="14,0,0,0" VerticalAlignment="Center">
                <Image Source="Assets/rune.ico" Width="16" Height="16"/>
                <TextBlock Text="Rune Setup" Foreground="#9CA3AF" FontSize="12" FontWeight="SemiBold" Margin="10,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>

            <Button Grid.Column="2" Style="{StaticResource CaptionButton}"
                    Content="&#x2715;" Click="CloseBtn_Click"/>
        </Grid>

        <!-- Accent line -->
        <Rectangle Grid.Row="1" Height="2" VerticalAlignment="Top"
                   Fill="#7C3AED" Opacity="0.9"/>

        <!-- Content -->
        <Grid Grid.Row="1" Margin="36,24,36,28">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,24">
                <TextBlock Text="Welcome to Rune Setup" FontSize="22" FontWeight="Bold" Foreground="#F0F2F5"/>
                <TextBlock Text="Install the Rune code editor and configure system shortcuts." FontSize="12.5" Foreground="#9CA3AF" Margin="0,6,0,0" LineHeight="18"/>
            </StackPanel>

            <!-- Content Area -->
            <Grid Grid.Row="1">
                <!-- Step 1: Options -->
                <StackPanel x:Name="Step1Panel" VerticalAlignment="Center">
                    <TextBlock Text="INSTALLATION OPTIONS" FontSize="11" FontWeight="Bold" Foreground="#6B7280" Margin="0,0,0,16"/>

                    <StackPanel Margin="0,0,0,20">
                        <TextBlock Text="Destination Folder" FontSize="12.5" FontWeight="SemiBold" Foreground="#F0F2F5" Margin="0,0,0,8"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="PathTextBox" VerticalContentAlignment="Center"/>
                            <Button Grid.Column="1" Content="Browse…" Margin="10,0,0,0" Padding="14,8" Click="BrowseBtn_Click"/>
                        </Grid>
                    </StackPanel>

                    <TextBlock Text="SHORTCUTS" FontSize="11" FontWeight="Bold" Foreground="#6B7280" Margin="0,0,0,12"/>
                    <CheckBox x:Name="DesktopShortcutCheck" Content="Create a desktop shortcut" IsChecked="True"/>
                    <CheckBox x:Name="StartMenuCheck" Content="Create a Start Menu shortcut" IsChecked="True" Margin="0"/>
                </StackPanel>

                <!-- Step 2: Installing -->
                <StackPanel x:Name="Step2Panel" VerticalAlignment="Center" Visibility="Collapsed">
                    <TextBlock Text="Installing Rune…" FontSize="17" FontWeight="SemiBold" Foreground="#F0F2F5" Margin="0,0,0,18"/>
                    <ProgressBar x:Name="InstallProgress" Height="6" IsIndeterminate="True" Margin="0,0,0,18"/>
                    <TextBlock x:Name="StatusText" Text="Extracting UI assets and creating configuration…" FontSize="12.5" Foreground="#9CA3AF" LineHeight="18"/>
                </StackPanel>

                <!-- Step 3: Finished -->
                <StackPanel x:Name="Step3Panel" VerticalAlignment="Center" Visibility="Collapsed">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <Border Width="36" Height="36" CornerRadius="18" Background="#10B981" Opacity="0.15" VerticalAlignment="Center">
                            <TextBlock Text="✓" Foreground="#10B981" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Margin="14,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="Installation Complete" FontSize="17" FontWeight="Bold" Foreground="#F0F2F5"/>
                            <TextBlock Text="Rune has been successfully installed." FontSize="12.5" Foreground="#9CA3AF" Margin="0,2,0,0"/>
                        </StackPanel>
                    </StackPanel>
                    <Border Background="#161922" CornerRadius="8" Padding="14" BorderBrush="#2C3242" BorderThickness="1">
                        <TextBlock x:Name="ReportText" FontSize="11.5" Foreground="#9CA3AF" TextWrapping="Wrap" FontFamily="Cascadia Code, Consolas, monospace" LineHeight="17"/>
                    </Border>
                </StackPanel>
            </Grid>

            <!-- Footer Buttons -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,24,0,0">
                <Button x:Name="CancelBtn" Content="Cancel" Background="#262B3A" Click="CancelBtn_Click" Margin="0,0,10,0" FontWeight="Medium"/>
                <Button x:Name="NextBtn" Content="Install" Click="NextBtn_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
